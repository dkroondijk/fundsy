<!-- use empty "" because we don't need/want to -->
<%= simple_form_for "", url: sessions_path, method: :post do |f| %>
  <%= f.input :email %>
  <%= f.input :password %>
  <%= f.submit "Login" %>
<% end %>
